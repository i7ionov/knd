def normalize_house_number(s):
    return s
    # пока без этого
    # s = s.translate(str.maketrans({x: None for x in [' ']}))
    # s = s.translate(str.maketrans({x: '\\' for x in ['/', ' ', '.', '-']}))
    # s.replace('к.')
    # return s
